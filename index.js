let add =(a, b) => {
    return a+b}
    ;
subtract=(a,b)=>a-b;
multiply=(a,b)=>a*b;
divide=(a,b)=>a/b;

increment=(n)=>n+1;
decrement=(n)=>n-1;

makeInt=(n)=>{
    return parseInt(n,10);
}

preserveDecimal=(n)=>{
return parseFloat(n);
}
